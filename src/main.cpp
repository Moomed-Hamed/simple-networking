#include "networking.h"

const char* ip   = NULL; // for example : "192.168.1.1"
const char* port = NULL; // for example : "42069"

int main()
{
	//server_demo(ip, port);
	//client_demo(ip, port);

	return 0;
}